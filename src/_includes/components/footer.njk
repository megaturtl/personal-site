<div id="buttons-banner">
    <marquee>
        {%- for button in collections.buttons -%}
            <img title="{{ button.name }}" src="{{ button.path }}" alt="{{ button.name }}">
        {%- endfor -%}
        {%- for button in collections.buttons -%}
            <img title="{{ button.name }}" src="{{ button.path }}" alt="{{ button.name }}">
        {%- endfor -%}
    </marquee>
</div>

<footer class="site-footer">
    <div id="stamps-banner">
        {%- for stamp in collections.stamps -%}
            <img title="{{ stamp.name }}" src="{{ stamp.path }}" alt="{{ stamp.name }}">
        {%- endfor -%}
    </div>

    <p>Congratulations! You scrolled further than 100% of users.</p>
    <div id="streamers"></div>

    <div class="footer-links">
        <a href="/credits">credits</a>
        <a href="/stats">stats</a>
        <a href="/terms">terms</a>
        <a href="/privacy">privacy</a>
    </div>
</footer>