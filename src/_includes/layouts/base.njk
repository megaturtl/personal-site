{# Base template for all normal pages to use #}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ description | default('My website') }}">
    <title>{% if title %}{{ title }} - {% endif %}{{ site.name }}</title>
    
    <!-- Preload critical fonts -->
    <link rel="preload" href="/assets/fonts/Author/Author-Variable.woff2" as="font" type="font/woff2" crossorigin>
    
    <!-- Global variables (sizing, typography) -->
    <link rel="stylesheet" href="/assets/css/vars.css">
    <!-- Base styles (layout, etc) -->
    <link rel="stylesheet" href="/assets/css/base.css">
    <!-- Component styles -->
    <link rel="stylesheet" href="/assets/css/components/header.css">
    <link rel="stylesheet" href="/assets/css/components/footer.css">
    <!-- Theme styles (colors only) -->
    <link rel="stylesheet" href="/assets/css/themes/light.css">
    <link rel="stylesheet" href="/assets/css/themes/dark.css">
    <link rel="stylesheet" href="/assets/css/themes/candy.css">
    <!-- Fonts -->
    <link rel="stylesheet" href="/assets/fonts/Author/font.css">
    <!-- Page-specific styles -->
    {% if pageCSS %}
    <link rel="stylesheet" href="/assets/css/pages/{{ pageCSS }}.css">
    {% endif %}

    <!-- Scripts -->
    <script src="/assets/js/theme-toggle.js" defer></script>
    <script src="/assets/js/lastfm-now-playing.js" defer></script>
</head>
<body>
    <div class="site-container">
        {% include "components/header.njk" %}

        <div class="page-content">
            <h2 class="page-title">{{ title }}</h2>
            {{ content | safe }}
        </div>

        {% include "components/footer.njk" %}
    </div>
</body>
</html> 